import {Unit} from '../Unit/Unit';
import normalize from './Functions/normalize';
import smooth from './Functions/transform';

export default (unitdata: Unit, data: object | object[]): object => {
    return {};
};
